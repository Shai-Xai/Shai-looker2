diff --git a/client/src/components/EventSignal.jsx b/client/src/components/EventSignal.jsx
index d893cbf..e7bcb8f 100644
--- a/client/src/components/EventSignal.jsx
+++ b/client/src/components/EventSignal.jsx
@@ -89,0 +90 @@ export function SignalBoard({ monitors }) {
+  const [pick, setPick] = useState(''); // monitor id filter: '' = whole site
@@ -97 +98,3 @@ export function SignalBoard({ monitors }) {
-      const r = roll.get(name);
+      // A monitor with no station split rolls all its devices into one ''
+      // entry — that belongs to the monitor's own tile.
+      const r = roll.get(name) || (st.station ? null : roll.get(''));
@@ -99 +102 @@ export function SignalBoard({ monitors }) {
-        name, zone: zoneOf(name), status: st.status, lagMin: st.lagMin, unit: unitFor(m), monitor: m.name,
+        name, zone: zoneOf(name), status: st.status, lagMin: st.lagMin, unit: unitFor(m), monitor: m.name, mid: m.id,
@@ -102,0 +106 @@ export function SignalBoard({ monitors }) {
+      if (r) roll.delete(r.station);
@@ -106,2 +110,2 @@ export function SignalBoard({ monitors }) {
-      if (r.station === '—') continue;
-      rows.push({ name: r.station, zone: zoneOf(r.station), status: r.on ? (r.off ? 'warn' : 'fresh') : 'stale', lagMin: null, unit: unitFor(m), monitor: m.name, on: r.on, off: r.off, txnH: r.txnH, spark: r.spark });
+      if (r.station === '—' || !r.station) continue;
+      rows.push({ name: r.station, zone: zoneOf(r.station), status: r.on ? (r.off ? 'warn' : 'fresh') : 'stale', lagMin: null, unit: unitFor(m), monitor: m.name, mid: m.id, on: r.on, off: r.off, txnH: r.txnH, spark: r.spark });
@@ -110,0 +115,6 @@ export function SignalBoard({ monitors }) {
+  // Filter chips — one per monitor (Bars / Vendors / Check-in), so the board
+  // can flick between station families without leaving the page.
+  const chipIcon = (m) => (m.area === 'Bar' ? '🍺' : m.area === 'Vendors' ? '🧾' : '🎟️');
+  const chips = open.filter((m) => rows.some((s) => s.mid === m.id));
+  const shown = pick ? rows.filter((s) => s.mid === pick) : rows;
+
@@ -112 +122 @@ export function SignalBoard({ monitors }) {
-  for (const s of rows) {
+  for (const s of shown) {
@@ -120,2 +130,2 @@ export function SignalBoard({ monitors }) {
-  const sum = (k) => rows.reduce((a, s) => a + (s[k] || 0), 0);
-  const units = new Set(rows.map((s) => s.unit));
+  const sum = (k) => shown.reduce((a, s) => a + (s[k] || 0), 0);
+  const units = new Set(shown.map((s) => s.unit));
@@ -124 +134 @@ export function SignalBoard({ monitors }) {
-    ['Stations', rows.length], ['Zones', zoneList.length],
+    ['Stations', shown.length], ['Zones', zoneList.length],
@@ -133,0 +144,6 @@ export function SignalBoard({ monitors }) {
+  const chipStyle = (act) => ({
+    border: `1px solid ${act ? 'var(--brand)' : 'var(--hairline)'}`, borderRadius: 999, cursor: 'pointer',
+    background: 'var(--card)', color: act ? 'var(--brand)' : 'var(--text)', fontWeight: act ? 800 : 600,
+    padding: '5px 12px', fontSize: 12, fontFamily: 'inherit', minHeight: 30,
+  });
+
@@ -135,0 +152,12 @@ export function SignalBoard({ monitors }) {
+      {/* monitor filter chips — split the board by station family */}
+      {chips.length > 1 && (
+        <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', marginBottom: 10 }}>
+          <button onClick={() => { setPick(''); setSel(null); }} style={chipStyle(!pick)}>All stations · {rows.length}</button>
+          {chips.map((m) => (
+            <button key={m.id} onClick={() => { setPick(m.id); setSel(null); }} style={chipStyle(pick === m.id)}>
+              {chipIcon(m)} {m.name} · {rows.filter((s) => s.mid === m.id).length}
+            </button>
+          ))}
+        </div>
+      )}
+
diff --git a/server/dataHealth.js b/server/dataHealth.js
index 47c491b..5afb351 100644
--- a/server/dataHealth.js
+++ b/server/dataHealth.js
@@ -999 +999 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
-          if (tl && m.stationField && tl.devices.length) {
+          if (tl && tl.devices.length) {
@@ -1001 +1001 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
-            const info3 = await deviceDetailsLite(m, a3
+            const info3 = !m.stationField ? null : await deviceDetailsLite(m, a3
@@ -1004,14 +1004,13 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
-            if (info3) {
-              const win = Math.max(1, Math.ceil(m.rosterOnlineMin / tl.intervalMin));
-              const hrN = Math.max(1, Math.round(60 / tl.intervalMin));
-              const byS = new Map();
-              for (const d of tl.devices) {
-                const stn = (info3.get(d.device) || {}).station || '—';
-                if (!byS.has(stn)) byS.set(stn, { station: stn, on: 0, off: 0, txnH: 0, spark: [0, 0, 0, 0, 0, 0] });
-                const e3 = byS.get(stn);
-                if (d.active.slice(-win).some(Boolean)) e3.on += 1; else e3.off += 1;
-                e3.txnH += d.counts.slice(-hrN).reduce((a3, b3) => a3 + b3, 0);
-                const s6 = d.counts.slice(-6);
-                s6.forEach((c3, i3) => { e3.spark[6 - s6.length + i3] += c3; });
-              }
-              stations = [...byS.values()].sort((a3, b3) => (b3.on + b3.off) - (a3.on + a3.off)).slice(0, 80);
+            const win = Math.max(1, Math.ceil(m.rosterOnlineMin / tl.intervalMin));
+            const hrN = Math.max(1, Math.round(60 / tl.intervalMin));
+            const byS = new Map();
+            for (const d of tl.devices) {
+              // Station-less monitors (e.g. one gate's check-in) lump into a
+              // single '' entry the board joins back to the monitor's own tile.
+              const stn = info3 ? ((info3.get(d.device) || {}).station || '—') : '';
+              if (!byS.has(stn)) byS.set(stn, { station: stn, on: 0, off: 0, txnH: 0, spark: [0, 0, 0, 0, 0, 0] });
+              const e3 = byS.get(stn);
+              if (d.active.slice(-win).some(Boolean)) e3.on += 1; else e3.off += 1;
+              e3.txnH += d.counts.slice(-hrN).reduce((a3, b3) => a3 + b3, 0);
+              const s6 = d.counts.slice(-6);
+              s6.forEach((c3, i3) => { e3.spark[6 - s6.length + i3] += c3; });
@@ -1018,0 +1018 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
+            stations = [...byS.values()].sort((a3, b3) => (b3.on + b3.off) - (a3.on + a3.off)).slice(0, 80);
diff --git a/test/dataHealth.test.js b/test/dataHealth.test.js
index cda5e9c..711a642 100644
--- a/test/dataHealth.test.js
+++ b/test/dataHealth.test.js
@@ -836,0 +837,29 @@ test('check() stores a per-station roll-up for the signal board', async () => {
+test('check() rolls a station-less monitor into one board entry', async () => {
+  const h = mountHealth();
+  const m = makeMonitor(h, { stationField: '', rosterField: 'scans.device_id', rosterOnlineMin: 30 });
+  const min10 = (minAgo) => new Date(Math.floor((Date.now() - minAgo * 60000) / 600000) * 600000).toISOString().slice(0, 16).replace('T', ' ');
+  h.setRowsFn(async (b) => {
+    if (b.fields.length === 1) return [{ 'scans.count': 9 }];                        // whole-feed total
+    if (b.fields.includes('data_health_latest')) return [{ data_health_latest: minsAgo(2) }];
+    if (b.fields.includes('data_health_last')) {
+      return [
+        { 'scans.device_id': 'D-1', data_health_last: minsAgo(2) },
+        { 'scans.device_id': 'D-2', data_health_last: minsAgo(120) },
+      ];
+    }
+    if (b.fields.includes('scans.count')) {
+      return [
+        { 'scans.device_id': 'D-1', 'scans.scanned_at_minute10': min10(0), 'scans.count': 4 },
+        { 'scans.device_id': 'D-2', 'scans.scanned_at_minute10': min10(120), 'scans.count': 3 },
+      ];
+    }
+    return [{ data_health_latest: minsAgo(2) }];
+  });
+  await h.mod.check(m);
+  const st = h.mod.monitorById(m.id).rosterSnapshot.stations;
+  assert.ok(Array.isArray(st));
+  assert.equal(st.length, 1);
+  assert.equal(st[0].station, '');
+  assert.equal(st[0].on, 1); assert.equal(st[0].off, 1); assert.equal(st[0].txnH, 4);
+});
+
