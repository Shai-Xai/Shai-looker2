diff --git a/client/src/components/DataHealthAdmin.jsx b/client/src/components/DataHealthAdmin.jsx
index 42d0732..955e35b 100644
--- a/client/src/components/DataHealthAdmin.jsx
+++ b/client/src/components/DataHealthAdmin.jsx
@@ -599 +599 @@ function MonitorCard({ m, entities = [], onChanged, onEdit, base = ADMIN_BASE, r
-              <div style={{ display: 'flex', alignItems: 'flex-end', gap: 3, height: 52 }}>
+              <div style={{ display: 'flex', alignItems: 'flex-end', gap: cov.length > 24 ? 1 : 3, height: 52 }}>
@@ -602 +602 @@ function MonitorCard({ m, entities = [], onChanged, onEdit, base = ADMIN_BASE, r
-                    style={{ flex: 1, maxWidth: 30, borderRadius: '3px 3px 0 0', height: c.n ? Math.max(3, Math.round((c.n / max) * 52)) : 3, background: c.n ? STATUS_COLOR.fresh : 'var(--hairline)' }} />
+                    style={{ flex: 1, maxWidth: cov.length > 24 ? 8 : 30, borderRadius: cov.length > 24 ? '1px 1px 0 0' : '3px 3px 0 0', height: c.n ? Math.max(3, Math.round((c.n / max) * 52)) : 3, background: c.n ? STATUS_COLOR.fresh : 'var(--hairline)' }} />
diff --git a/server/dataHealth.js b/server/dataHealth.js
index d1e5302..6835a73 100644
--- a/server/dataHealth.js
+++ b/server/dataHealth.js
@@ -836,0 +837 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
+        const hourlyN = []; // devices seen per HOUR — keeps the flow score stable at fine blocks
@@ -838,4 +839,3 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
-          let t = await deviceTimeline(m, rosterAnchor(m) ? 'start' : 12, 60);
-          // Row-presence fallback at hour blocks = one row per device-hour, a
-          // wild undercount — re-read at 10-min blocks where a raw row ≈ a scan.
-          if (t.configured && t.countBasis === 'none') t = await deviceTimeline(m, rosterAnchor(m) ? 'start' : 12, 10);
+          // 10-min blocks: the tile graph shows the day at the same resolution
+          // as the live timeline (hour bars hid the short dropouts).
+          const t = await deviceTimeline(m, rosterAnchor(m) ? 'start' : 12, 10);
@@ -849,0 +850,7 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
+            // A device counts as "on" for an hour if ANY of its blocks in that
+            // hour sent data — a bar selling hourly isn't down five blocks of six.
+            const perH = Math.max(1, Math.round(60 / t.intervalMin));
+            for (let i = 0; i < t.buckets.length; i += perH) {
+              const end = Math.min(i + perH, t.buckets.length);
+              hourlyN.push(t.devices.reduce((a, d) => a + (d.active.slice(i, end).some((x) => x) ? 1 : 0), 0));
+            }
@@ -858,3 +865,3 @@ function mount(app, { db, auth, looker, runLookerQuery, applyScope, os, ops, mai
-        if (coverage && coverage.length && r.total) {
-          const uptime = coverage.reduce((a, c) => a + Math.min(1, c.n / r.total), 0) / coverage.length;
-          const continuity = coverage.filter((c) => c.n > 0).length / coverage.length;
+        if (hourlyN.length && r.total) {
+          const uptime = hourlyN.reduce((a, n) => a + Math.min(1, n / r.total), 0) / hourlyN.length;
+          const continuity = hourlyN.filter((n) => n > 0).length / hourlyN.length;
