diff --git a/client/src/components/EventOpsConsole.jsx b/client/src/components/EventOpsConsole.jsx
index c0180af..807a37f 100644
--- a/client/src/components/EventOpsConsole.jsx
+++ b/client/src/components/EventOpsConsole.jsx
@@ -1249 +1249 @@ function StaffTab({ suiteId, canManage, flash, reloadKey, onDevice }) {
-      const body = { name: form.name, number: form.number, role: form.role, stationIds: form.stationIds || [], canMove: !!form.canMove, canCheckpoint: !!form.canCheckpoint };
+      const body = { name: form.name, number: form.number, role: form.role, stationIds: form.stationIds || [], canMove: !!form.canMove, canCheckpoint: !!form.canCheckpoint, alertsOn: form.alertsOn !== false };
@@ -1276 +1276 @@ function StaffTab({ suiteId, canManage, flash, reloadKey, onDevice }) {
-      {canManage && <button onClick={() => setForm({ name: '', number: '', role: '', stationIds: [], canMove: true, canCheckpoint: false })} style={primaryBtn}>＋ Add staff</button>}
+      {canManage && <button onClick={() => setForm({ name: '', number: '', role: '', stationIds: [], canMove: true, canCheckpoint: false, alertsOn: true })} style={primaryBtn}>＋ Add staff</button>}
@@ -1304 +1304 @@ function StaffTab({ suiteId, canManage, flash, reloadKey, onDevice }) {
-                    <button onClick={() => setForm({ id: s.id, name: s.name, number: s.number, role: s.role, stationIds: s.stationIds || [], canMove: s.canMove !== false, canCheckpoint: !!s.canCheckpoint })} style={iconBtn}>✏️</button>
+                    <button onClick={() => setForm({ id: s.id, name: s.name, number: s.number, role: s.role, stationIds: s.stationIds || [], canMove: s.canMove !== false, canCheckpoint: !!s.canCheckpoint, alertsOn: s.alertsOn !== false })} style={iconBtn}>✏️</button>
@@ -1340,0 +1341 @@ function StaffTab({ suiteId, canManage, flash, reloadKey, onDevice }) {
+                <Chip on={form.alertsOn !== false} onClick={() => setForm({ ...form, alertsOn: form.alertsOn === false })}>🔔 Station alerts</Chip>
diff --git a/server/eventops.js b/server/eventops.js
index 8596b23..83c4510 100644
--- a/server/eventops.js
+++ b/server/eventops.js
@@ -229,0 +230,2 @@ function mount(app, { db, auth, push = require('./push'), messaging = null }) {
+  // Station alerts for this staffer (default ON): a dark station pages them.
+  try { sql.exec('ALTER TABLE eventops_staff ADD COLUMN alerts_on INTEGER NOT NULL DEFAULT 1'); } catch { /* already there */ }
@@ -316,0 +319 @@ function mount(app, { db, auth, push = require('./push'), messaging = null }) {
+      alertsOn: s.alerts_on == null ? true : !!s.alerts_on,
@@ -800 +803 @@ function mount(app, { db, auth, push = require('./push'), messaging = null }) {
-      canMove: bool(b.canMove, 1), canCheckpoint: bool(b.canCheckpoint, 0) };
+      canMove: bool(b.canMove, 1), canCheckpoint: bool(b.canCheckpoint, 0), alertsOn: bool(b.alertsOn, 1) };
@@ -808,2 +811,2 @@ function mount(app, { db, auth, push = require('./push'), messaging = null }) {
-    sql.prepare('INSERT INTO eventops_staff (id, entity_id, suite_id, name, number, role, station_id, station_ids, can_move, can_checkpoint, created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?)')
-      .run(id, su.entityId, su.id, c.name, c.number, c.role, c.stationIds[0] || '', JSON.stringify(c.stationIds), c.canMove, c.canCheckpoint, now());
+    sql.prepare('INSERT INTO eventops_staff (id, entity_id, suite_id, name, number, role, station_id, station_ids, can_move, can_checkpoint, alerts_on, created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)')
+      .run(id, su.entityId, su.id, c.name, c.number, c.role, c.stationIds[0] || '', JSON.stringify(c.stationIds), c.canMove, c.canCheckpoint, c.alertsOn, now());
@@ -819,3 +822,3 @@ function mount(app, { db, auth, push = require('./push'), messaging = null }) {
-      canMove: req.body?.canMove ?? cur.canMove, canCheckpoint: req.body?.canCheckpoint ?? cur.canCheckpoint }, su);
-    sql.prepare('UPDATE eventops_staff SET name=?, number=?, role=?, station_id=?, station_ids=?, can_move=?, can_checkpoint=? WHERE id=?')
-      .run(c.name, c.number, c.role, c.stationIds[0] || '', JSON.stringify(c.stationIds), c.canMove, c.canCheckpoint, s.id);
+      canMove: req.body?.canMove ?? cur.canMove, canCheckpoint: req.body?.canCheckpoint ?? cur.canCheckpoint, alertsOn: req.body?.alertsOn ?? cur.alertsOn }, su);
+    sql.prepare('UPDATE eventops_staff SET name=?, number=?, role=?, station_id=?, station_ids=?, can_move=?, can_checkpoint=?, alerts_on=? WHERE id=?')
+      .run(c.name, c.number, c.role, c.stationIds[0] || '', JSON.stringify(c.stationIds), c.canMove, c.canCheckpoint, c.alertsOn, s.id);
diff --git a/server/staffAlerts.js b/server/staffAlerts.js
index 8e0379c..af97abc 100644
--- a/server/staffAlerts.js
+++ b/server/staffAlerts.js
@@ -209 +209 @@ function mount(app, { db, auth, mailer = require('./mailer'), push = require('./
-        const staff = sql.prepare('SELECT id, name, number, role, station_id FROM eventops_staff WHERE suite_id=?').all(suiteId);
+        const staff = sql.prepare('SELECT id, name, number, role, station_id FROM eventops_staff WHERE suite_id=? AND (alerts_on IS NULL OR alerts_on=1)').all(suiteId);
